services:
  app:
    build: .
    ports:
      - 3001:3001
    volumes:
      - .:/node/app
    restart: on-failure:5
    depends_on:
      - db
  db:
    image: postgres
    restart: always
    env_file:
      - .env
    ports:
      - "5432:5432"
